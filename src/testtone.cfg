MEMORY {
    ZP:      start = $0002, size = $00FE, type = rw;
    HEADER:  start = $1001, size = $000C, type = ro, file = %O, fill = yes, fillval = $00;
    STARTUP: start = $100D, size = $1FF3, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    BASIC:   load = HEADER,  type = ro;
    CODE:    load = STARTUP, type = ro;
}
